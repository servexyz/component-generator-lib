{
  "name": "component-generator-lib",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && npm run test",
    "build": "babel src --out-dir dist --copy-files",
    "test": "jest"
  },
  "dependencies": {
    "chalk": "^2.2.0",
    "mkdirp": "^0.5.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.57.3",
    "jest": "^21.2.1"
  },
  "jest": {
    "verbose": true,
    "bail": true,
    "modulePathIgnorePatterns": [
      "deleteMe"
    ]
  }
}
