const { generator, createDirectory, createFile, createFiles, deleteFiles } = require("./dist/generator");

module.exports = {
  generator,
  createDirectory,
  createFile,
  createFiles,
  deleteFiles
}
